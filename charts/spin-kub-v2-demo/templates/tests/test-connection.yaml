apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spin-kub-v2-demo.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "spin-kub-v2-demo.name" . }}
    helm.sh/chart: {{ include "spin-kub-v2-demo.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "spin-kub-v2-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
